/**
 * Extends the {@link gui.Gui} interface with
 * "{@link gui.component.Component}s", which are objects that draw themselves on
 * the GUI and can react to local user inputs using simple callbacks.
 */
package gui.component;