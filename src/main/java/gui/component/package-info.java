/**
 * Extends the {@link gui.Window} interface with
 * "{@link gui.component.Component}s", which are objects that draw themselves on
 * the window and can react to local user inputs using simple callbacks.
 */
package gui.component;